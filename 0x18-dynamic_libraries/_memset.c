#include "main.h"

/**
 * _memset - setup memory
 * @s: I don't know
 * @b: Same thing
 * @n: Same thing
 *
 * Return: I don't know yet
 *
 * By: Sina Mathew
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
